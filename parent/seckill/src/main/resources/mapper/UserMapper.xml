<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dzx.seckill.dao.UserDao">
    <select id="selectUserById" parameterType="java.lang.Integer" resultType="com.dzx.seckill.entity.User">
        SELECT id,name FROM t_user WHERE id = #{userID}
    </select>
</mapper>